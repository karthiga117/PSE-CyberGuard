# Example CyberGuard DSL Files

This directory contains example DSL files for CyberGuard.

## Future Examples (Phase 2+)

Once the DSL syntax and interpreter are implemented, this directory will contain:

- `authentication.cg` - Example API authentication testing
- `api_security.cg` - Example API security validation
- `cloud_aws.cg` - Example AWS security checks
- `cloud_azure.cg` - Example Azure security checks

## Status

Currently empty. Examples will be added in Phase 2 when the DSL language specification is complete.
